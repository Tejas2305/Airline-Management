<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GalaxyAirlines"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">
        
        <!-- Main Activity (Splash Screen) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.GalaxyAirlines.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Landing Activity -->
        <activity
            android:name=".ui.LandingActivity"
            android:theme="@style/Theme.GalaxyAirlines.NoActionBar" />
            
        <!-- Login Activity -->
        <activity
            android:name=".ui.LoginActivity"
            android:theme="@style/Theme.GalaxyAirlines.NoActionBar" />
            
        <!-- User Dashboard Activity -->
        <activity
            android:name=".ui.UserDashboardActivity"
            android:theme="@style/Theme.GalaxyAirlines.NoActionBar" />
            
        <!-- Admin Dashboard Activity -->
        <activity
            android:name=".ui.AdminDashboardActivity"
            android:theme="@style/Theme.GalaxyAirlines.NoActionBar" />
            
        <!-- Flight Search Activity -->
        <activity
            android:name=".ui.FlightSearchActivity"
            android:theme="@style/Theme.GalaxyAirlines.NoActionBar" />
            
        <!-- Flight Results Activity -->
        <activity
            android:name=".ui.FlightResultsActivity"
            android:theme="@style/Theme.GalaxyAirlines.NoActionBar" />
            
    </application>

</manifest>